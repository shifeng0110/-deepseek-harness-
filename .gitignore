# runtime asset dir for the livewall plugin (large user media lives here)
bg-assets/

# stray diagnostics
livewall/.boot.log

# whale runtime config holds the user API key - keep it local
whale/config.json
